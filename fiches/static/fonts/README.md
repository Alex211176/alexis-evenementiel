# Polices custom

Dépose ici tes fichiers de police custom (de préférence `.woff2`).

Puis active-les dans `data/defaults/prestataire.json`, section `prestataire.fonts`.

Voir `docs/BRANDING.md` pour le détail.
