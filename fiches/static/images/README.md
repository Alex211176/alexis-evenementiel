# Images & logo

Dépose ici ton logo (de préférence `.svg`, sinon `.png` haute résolution).

Puis active-le dans `data/defaults/prestataire.json`, section `prestataire.brand` :

```json
"brand": {
    "logo_file": "logo.svg",
    ...
}
```

Voir `docs/BRANDING.md` pour le détail.
