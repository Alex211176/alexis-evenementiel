"""
poses/ — Module « Poses Mariage » (sélection client + check-list jour J).

Blueprint additif greffé sur l'app Alexis Événementiel existante. Ne modifie
aucune logique devis/fiches : il réutilise la couche `storage` et le service
Render déjà en place.

Jalon 0 : bibliothèque embarquée + affichage lecture seule par phase.
"""
